# TODO

Keep items priority-ordered and current.

- Migrate Python tooling to match `agent-config` conventions by adding Ruff and pytest configuration/checks.
- Continue package cleanup by breaking down `AudioEngine._handle_command` into smaller command/session helpers after smoke testing.
